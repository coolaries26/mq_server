Cluster
Ease of administration
High availability
load balancing

MQ SUPPORT 2 PARENTS 
	QM1					QM2					QM3
	 P					 C					 C		*PARENT CHILD
	 F					 P					 P		*FULL AND PARTIAL REPOSITORIES
	CLUS1				CLUS2				CLUS3	* CRTMQM
	
				ALTER QMGR REPOS(BANK) 	
	RUNMQSC CLUS1
	ALTER QMGR REPOS(BANK)
	DEF LISTENER(LST1) TRPTYP(TCP) PORT(4000)
	
	RUNMQSC CLUS2
*	ALTER QMGR REPOS(BANK)
	DEF LISTENER(LST1) TRPTYP(TCP) PORT(4001)
	
	RUNMQSC CLUS3
	DEF LISTENER(LST1) TRPTYP(TCP) PORT(4003)

	FOR PARENT NODE
		DEF CHL(TO.CLUS1) CHLTYPE(CLUSRCVR) CONNAME('LOCALHOST(4000)') CLUSTER(BANK)
	FOR CHILD NODE
		RUNMQSC CLUS2
		DEF CHL(TO.CLUS2) CHLTYPE(CLUSRCVR) CONNAME('LOCALHOST(4001)') CLUSTER(BANK)
		DEF CHL(TO.CLUS1) CHLTYPE(CLUSSDR) CONNAME('LOCALHOST(4000)') CLUSTER(BANK)
		RUNMQSC CLUS3
		DEF CHL(TO.CLUS3) CHLTYPE(CLUSRCVR) CONNAME('LOCALHOST(4003)') CLUSTER(BANK)
		DEF CHL(TO.CLUS1) CHLTYPE(CLUSSDR) CONNAME('LOCALHOST(4000)') CLUSTER(BANK)
		DEF QL(CLUSQ) CLUSTER(BANK)
DIS CLUSQ
DIS CLUSTER(*)